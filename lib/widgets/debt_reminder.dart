import 'package:flutter/material.dart';

class DebtReminder extends StatelessWidget {
  const DebtReminder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
